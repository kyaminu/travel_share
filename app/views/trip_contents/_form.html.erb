<%= form_with(model: [Trip.find(params[:trip_id]), trip_content]) do |form| %>
  <% if trip_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trip_content.errors.count, "error") %> prohibited this trip_content from being saved:</h2>

      <ul>
        <% trip_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :timestamp, style: "display: block" %>
    <%= form.datetime_field :timestamp %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
