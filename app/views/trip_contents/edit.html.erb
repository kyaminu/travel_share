<% content_for(:title, "予定を変更") %>

<div class="d-flex justify-content-between">
  <h2 class="mt-3 ms-3">予定を変更する</h2>
  <%= link_to "詳細画面に戻る", trip_path(@trip.id), class:"m-3 btn btn-dark" %>
</div>

<%= render "form", trip_content: @trip_content %>