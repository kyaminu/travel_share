<!--<% content_for(:title, @trip.trip_types) %>-->

<!--showの内容-->
<div class="container-fluid">
  <div class="row">
    <h2 class="my-3 mx-5 ms-4 col-4">
      <%= "行き先: #{Trip.trip_types.keys[@trip.name]}" %>
    </h2>
    
    <div class="mt-3 col-4 offset-10">
      <%= link_to "行き先を変更する", edit_trip_path(@trip), class:"btn btn-outline-primary" %>
      <%= link_to "一覧に戻る", trips_path, class:"btn btn-dark ms-1"%>
    </div>
  </div>
</div>

<div class-"container">
  <div class="row">
    <div class="col-6">
      <div class="trip_contents_text_area_margin">
        <% @trip.trip_contents.each do |trip| %>
          <div class="mb-3">
             <%= l trip.timestamp, format: :short %></br>
             <div class="border rounded-3 bg-light trip_contents_text_area">
               <%= trip.content%>
             </div>
             <%= link_to "編集", "#", class:"btn btn-outline-secondary btn-sm"%>
             <%= link_to "削除", "#", class:"btn btn-outline-danger btn-sm"%></br>
             <!--<%= link_to "編集", edit_trip_trip_content_path(@trip)%>-->
             <!--<%= link_to "削除", trip_path(@trip), method: :delete%>-->
          </div>
        <% end %>
      </div>
    </div>
    
    <div class="col offset text-center mt-3">  
      <!--inputの内容-->
      <%= form_with url: trip_trip_contents_path(@trip), local: true do |form| %>
<% 
=begin
%>
  <% if trip_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trip_content.errors.count, "error") %> prohibited this trip_content from being saved:</h2>

      <ul>
        <% trip_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%
=end
%>
        <div>
          <%= form.datetime_field :timestamp,class:"input_trip_contents_datetime_field border rounded-3 bg-light" %></br>
          <%= form.text_area :content, class:"input_trip_contents_text_area border rounded-3" %></br>
          <%= form.submit "予定を追加する", class:"mt-3 btn btn-success" %>
        </div>
      <% end %>
      </div>
    </div>
  </div>
</div>
<%= button_to "この旅程を削除する", @trip, method: :delete, class:"btn btn-outline-danger me-5 mb-3 float-end" %>